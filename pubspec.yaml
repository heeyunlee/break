name: workout_player
description: A new app that helps you workout better.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.1.3

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_svg: null
  intl: ^0.17.0
  http: ^0.13.1
  firebase_storage: ^8.0.0
  firebase_core: ^1.0.1
  cloud_firestore: ^1.0.1
  firebase_auth: ^1.0.1
  firebase_crashlytics: ^1.0.0 # no web
  firebase_analytics: ^7.1.1
  flutter_facebook_auth: ^3.1.1
  sign_in_with_apple: ^3.0.0 ## no web
  google_sign_in: ^5.0.1
  fl_chart: ^0.30.0
  google_fonts: ^2.0.0
  smooth_page_indicator: ^0.3.0-nullsafety.0
  provider: ^5.0.0
  cupertino_icons: ^1.0.2
  logger: ^1.0.0
  persistent_bottom_nav_bar: ^4.0.0
  rxdart: ^0.26.0
  flutter_slidable: ^0.6.0-nullsafety.0
  circular_countdown_timer: ^0.1.0 # no null safety
  keyboard_actions: ^3.4.0
  flutter_cache_manager: ^3.0.0-nullsafety.1 # no web
  cached_network_image: ^3.0.0-nullsafety # no web
  font_awesome_flutter: ^9.0.0
  pedantic: ^1.11.0
  paginate_firestore: ^1.0.0-nullsafety.0
  flutter_rating_bar: ^4.0.0
  confetti: ^0.6.0-nullsafety
  flutter_speed_dial: ^3.0.5
  numberpicker: ^2.0.1
  # flutter_kakao_login: ^3.1.0 # no web
  shimmer: ^2.0.0-nullsafety.0
  # dio: ^3.0.9
  # json_serializable: ^3.3.0
  # shared_preferences: ^2.0.3
  # kakao_flutter_sdk: ^0.6.0-beta.2 # no null safety
  # dio: ^4.0.0-beta6
  # json_serializable: ^4.0.0
  # shared_preferences: ^2.0.5
  flutter_kakao_login: ^3.1.0
  dotted_line: ^3.0.0
  miniplayer: ^1.0.0+2
  timeago: ^3.0.2
dev_dependencies:
  flutter_launcher_icons: ^0.9.0
  flutter_test:
    sdk: flutter

flutter_icons:
  image_path: "assets/logos/playerh_logo.png"
  andriod: true
  ios: true
  adaptive_icon_background: "#000000"
  adaptive_icon_foreground: "#000000"

flutter:
  fonts:
    - family: NanumSquareRound
      fonts:
        - asset: assets/fonts/NanumSquareRoundB.ttf
          weight: 700
        - asset: assets/fonts/NanumSquareRoundEB.ttf
          weight: 800
        - asset: assets/fonts/NanumSquareRoundL.ttf
          weight: 300
        - asset: assets/fonts/NanumSquareRoundR.ttf
          weight: 400
    - family: Roboto
      fonts:
        - asset: assets/fonts/Roboto-Medium.ttf
          weight: 400

  uses-material-design: true
  assets:
    - assets/fonts/
    - assets/icons/
    - assets/logos/
    - assets/images/
    - assets/emojis/
flutter_intl:
  enabled: true
